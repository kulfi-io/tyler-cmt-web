export default class Note {
    constructor(title, subject, body, userId, id) {
        this.title = title;
        this.subject = subject;
        this.body = body;
        this.userId = userId;
        this.id = id;
    }
}
//# sourceMappingURL=note.js.map